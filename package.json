{
  "name": "dune-design",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "chmod +x ./node_modules/.bin/vite && vite build"
    "dev": "vite",
    "build": "vite build"
  },
  "engines": {
    "node": ">=20.x"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^7.2.2"
  }
}
